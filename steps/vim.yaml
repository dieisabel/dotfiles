# Dotbot directives for configuring Vim

- create:
    - ~/.config/vim/

- link:
    ~/.config/vim/:
      glob: true
      path: vim/*
      exclude: [ install ]

- shell:
    - command: bash vim/install
      description: Installing plugins
      stdout: true
      stderr: true
