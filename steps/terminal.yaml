# Dotbot directives for configuring terminal (Alacritty)

- defaults:
    link:
      # Remove old target and create a new link
      force: true

- create:
    - ~/.config/alacritty/

- link:
    ~/.config/alacritty/alacritty.yml: terminal/alacritty.yml
