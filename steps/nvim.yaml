# Dotbot directives for configuring Neovim

- defaults:
    link:
      # Remove old target and create a new link
      force: true

- create:
    - ~/.config/nvim/

- link:
    ~/.config/nvim/:
      glob: true
      path: nvim/*
